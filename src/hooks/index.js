// hooks.js
import { useDispatch, useSelector } from "react-redux";

// Custom hooks (JavaScript version – no types)
export const useAppDispatch = useDispatch;
export const useAppSelector = useSelector;
